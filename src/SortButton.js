/* eslint-disable indent, linebreak-style, comma-dangle */
import React from "react";

const SortButton = ({ clickHandler }) => {
  return <button onClick={clickHandler}>Toggle sorting</button>;
};

export default SortButton;
